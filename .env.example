# Frontend environment variables (copy to .env.local for local dev)

# URL of the Django backend API
VITE_API_URL=http://127.0.0.1:8000/api

# Optional: Gemini API key, used by the AI-assist buttons in the Admin console
GEMINI_API_KEY=
